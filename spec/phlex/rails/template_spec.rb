# frozen_string_literal: true

RSpec.describe Phlex::Rails::Template do
  it "has a version number" do
    expect(Phlex::Rails::Template::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
